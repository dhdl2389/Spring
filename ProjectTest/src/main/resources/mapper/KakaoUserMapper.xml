<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.testing.KakaoUserMapper">

   <insert id="insertUser" parameterType="KakaoUserDTO">
        INSERT INTO kakaousertbl (user_code, id, nickname, profile_image)
        VALUES (seq_kakao_code.nextval, #{id}, #{nickname}, #{profile_image})
    </insert>

</mapper>